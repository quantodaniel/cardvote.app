export const useRoomData = (roomId: string) => {
  const getRoomData = async () => {};
  const updateEstimate = async (card: number, estimate: number) => {};
  const clearEstimates = async () => {};

  return { getRoomData, updateEstimate, clearEstimates };
};
